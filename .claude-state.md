# Estado — Projeto Buscador de Vagas (Aumento de Salário)

Atualizado: 2026-09-10

## Onde estamos
- Processo Normal rodado. Abordagem **B** aprovada (buscador 3 camadas, 2 ondas).
- Fase 2 (planejamento) concluída — ver `docs/SPEC.md`.
- **NA NUVEM desde 2026-09-10** (Processo Normal; venc.: GitHub Actions + Pages). Repo público
  `kenobii/buscador-vagas`. Roda sozinho todo dia (cron 12:00 UTC / 09:00 BRT) + botão manual.
  Chaves nos GitHub Secrets. Página pública: https://kenobii.github.io/buscador-vagas/ (PROVADA HTTP 200, sem dado pessoal).
  Perfil da nuvem = `perfil_busca.json` (só objetivo+habilidades); `curriculo_mestre.json`/PDF/.env ficam FORA do repo (`.gitignore`).
- **Próximo:** nada pendente. Rodar na mão quando quiser: aba Actions do repo → "Atualiza vagas" → Run workflow.

## Já construído (Fase 2, 1ª versão)
- `buscar_vagas.py` — busca Adzuna + pontuação (0-100) + Excel ordenado. PROVADO no `--demo`.
- `Buscar Vagas.bat`, `requirements.txt`, `COMO_USAR.html` (guia).
- `curriculo_mestre.json` (perfil). Falta `gerar_curriculo.py` (não existe; não recriar sem pedir).

## Chaves (via coletor `~/.claude/tools/coletar-chave.py`, gravam no .env do projeto)
- `ADZUNA_APP_ID` + `ADZUNA_APP_KEY` — SALVOS no `.env` em 2026-09-10 (via coletor).
- `JOOBLE_API_KEY` — Ygor cadastra em jooble.org/api/about. PENDENTE (onda 1, E2).

## Onda 1 — etapas
- [x] E1 refatorar multi-fonte (Adzuna módulo) — PROVADO --demo (100/77/8)
- [x] E2 fonte Jooble (opcional por chave) — pula sem chave, provado
- [x] E3 aba "Links pra clicar" (grandes) — 10 links, LinkedIn conferido
- [x] E4 atualizar guia HTML + prova --demo ponta a ponta — 2 abas OK
- [x] E5 rodar real com as chaves do Ygor — FEITO 2026-09-10: Adzuna App ID+Key salvos no `.env`; rodada real trouxe 162 vagas, página em `site/index.html`. Jooble segue vazio (opcional).

## Notas
- Iniciante: explicar comandos antes de rodar. Respostas curtas (Feito/Decisão/Próximo).
- Ideação salva em scratchpad (ideacao_*.md + veredito_gemini.md).
